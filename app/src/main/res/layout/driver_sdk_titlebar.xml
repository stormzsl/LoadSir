<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/main_layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="48dp"
    android:background="@color/driver_sdk_title_bar_background_normal"
    android:descendantFocusability="blocksDescendants">

    <!-- 标题栏左侧布局 -->

    <LinearLayout
        android:id="@+id/layout_top_back"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:background="@drawable/driver_sdk_selector_title_bar_button"
        android:orientation="horizontal"
        android:gravity="center"
        android:clickable="true"/>

    <!-- 标题栏中间布局 -->
    <LinearLayout
        android:id="@+id/layout_top_middle"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:orientation="horizontal"
        android:gravity="center"/>

    <!-- 标题栏右侧布局 -->

    <LinearLayout
        android:id="@+id/layout_top_right"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@drawable/driver_sdk_selector_title_bar_button"
        android:clickable="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_alignParentEnd="true" />

    <TextView
        android:id="@+id/txt_debug"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:textColor="@android:color/white"
        android:textSize="8sp"
        android:visibility="gone"
        android:paddingEnd="5dp"
        android:layout_alignParentEnd="true"
        android:paddingStart="5dp" />

</RelativeLayout>
